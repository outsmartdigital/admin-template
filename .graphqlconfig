{
  "schemaPath": "schema.graphql",
  "projects": {
    "default": {
      "extensions": {
        "endpoints": {
          "dev": {
            "url": "${env:NEXT_PUBLIC_GRAPHQL_URL}",
            "schemaPath": "schema.graphql",
            "headers": {
              "Authorization": "Bearer ${env:NEXT_PUBLIC_PUBLIC_TOKEN}"
            }
          }
        }
      }
    }
  }
}
